# Legacy compatibility plan

The original AlecaFrame-backed application remains compatible with the current data
platform. The active delivery work is tracked only in [the delivery checklist](../todo.md).

The product remains local and read-only: it does not automate gameplay, create market
orders, operate a remote service, or expose a network listener for MCP.
