# F77 — filtro de recompensa consistente

F77 aplica o mesmo filtro textual de recompensa ao `get_bounties`, além de
`get_activity` e `get_world_state`. O filtro é case-insensitive, limitado a 200
caracteres e só considera recompensas presentes na revisão atual. A resposta não
projeta bounties sem recompensa observada e continua carregando estado, validade,
cobertura e revisão conforme a ferramenta usada.

Validação automatizada:

- `get_bounties(reward=endo)` retorna `deimos-f33` na fixture;
- MCP: 13/13 testes;
- Core: 142/142 testes;
- build do MCP sem warnings nem erros.
