# Validação F194 — clientes reais e smoke econômico no pacote 0.0.9

## Clientes MCP

As configurações globais do Codex e Claude foram atualizadas para:

```text
artifacts/MyFrame-0.0.9-win-x64/MyFrame.Mcp.exe
```

O smoke real executou `get_capabilities` nos dois clientes:

```text
Codex:  my-frame — 23 tools
Claude: my-frame — 23 tools
```

## Pacote distribuído

O script `scripts/Test-EconomicInspector.ps1` passou a aceitar `-SyncPath` e
`-ServerPath` e foi executado apontando diretamente para os binários 0.0.9.

```text
overwolf-inventory: published, imported=1, rejected=0
public-export: published, records=1
get_overview: prices=valid, orders=valid, availabilityConfirmed=True
list_sales: count=2, platinum=15, ducats=15, complete=True
  Chassis: action=platinum, reserved=1, available=1, each=15, total=15
  Systems: action=ducats, price=null
list_surplus: count=2, platinum=15, ducats=30, complete=True
```

## Critérios

- [x] Codex usa o MCP distribuído 0.0.9.
- [x] Claude usa o MCP distribuído 0.0.9.
- [x] Ambos negociam e chamam `get_capabilities`.
- [x] SyncHost e MCP distribuídos completam o fluxo econômico.
- [x] Preço ausente permanece nulo; reserva de ordem reduz a disponibilidade.
- [ ] Capturar dados reais do inventário do Warframe em execução.
