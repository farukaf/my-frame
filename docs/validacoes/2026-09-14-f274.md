# F274 — smoke real de `get_inventory_changes`

Codex e Claude chamaram uma vez `get_inventory_changes` no MCP 0.0.24, sem
argumentos e sem outras ferramentas.

Ambos retornaram `state=not_initialized` e `items=[]`. O resultado é coerente:
não há revisões reais do coletor para comparar; não significa inventário vazio.
Claude recebeu a ferramenta explicitamente permitida e não teve negações.
