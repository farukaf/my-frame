# F335 — contrato central do coletor atualizado

`docs/COLETOR-OVERWOLF.md` agora descreve `contextId` opcional, a distinção entre snapshots e
deltas e o estado `context_mismatch` do MCP. O gate de captura real permanece explícito:
heartbeat, marker e comparação manual com o Arsenal ainda são necessários para promover campos a
fatos observados.
