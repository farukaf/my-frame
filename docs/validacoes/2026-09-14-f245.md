# F245 — distribuição 0.0.17 e atualização dos clientes

A distribuição `MyFrame-0.0.17-win-x64` foi publicada com as mudanças da F243/F244.

Validações executadas:

- upgrade side-by-side 0.0.16 → 0.0.17: `DISTRIBUTION_UPGRADE_OK=1`;
- smoke read-only do pacote MCP: `MCP_READONLY_OK=1`, `MCP_NETWORK_CONNECTIONS=0`, `MCP_EXIT=0`;
- Codex e Claude atualizados para o executável 0.0.17 e reportados como conectados.

A homologação F1 do coletor continua pendente: a extensão Overwolf ainda precisa ser
carregada como unpacked enquanto o Warframe estiver em execução, para produzir heartbeat
e marcadores reais.
