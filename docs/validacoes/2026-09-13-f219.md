# F219 — regressão da solução após cobertura de aquisição

## Validação

Executado `scripts/Test-SolutionRegression.ps1` em modo sequencial após F218:

- `MyFrame.Core.Tests`: 180 testes aprovados;
- `MyFrame.Mcp.Tests`: 22 testes aprovados;
- `SOLUTION_REGRESSION_OK=1` e dois projetos processados.

Os avisos NU1900 refletem a indisponibilidade do proxy/NuGet neste ambiente e
não impediram a compilação com dependências já restauradas.
