# Validação F138 — orquestrador World State compartilhado

`WorldStateSyncRunner` passou a ser a fronteira comum do App e do
`MyFrame.Sync`, incluindo publicação, leitura de status, parser version e
política explícita de fallback comunitário.

Validações:

```text
MyFrame.Core.Tests: 162/162
MyFrame.App build: 0 erros, 0 warnings
MyFrame.Sync build: 0 erros, 0 warnings
```

O teste de integração usa JSON fixture, publica em SQLite temporário e confirma
`worldstate-official-1`. A execução real permanece sujeita ao bloqueio de rede
documentado nas fases anteriores.
