# F236 — smoke read-only do MCP distribuído 0.0.15

## Validação

Contra `artifacts/MyFrame-0.0.15-win-x64/MyFrame.Mcp.exe`:

- `MCP_READONLY_OK=1`;
- `MCP_NETWORK_CONNECTIONS=0`;
- `MCP_EXIT=0`;
- stdout permaneceu vazio antes de uma requisição MCP;
- nenhuma raiz de dados limpa foi criada ou modificada.

Isso confirma o gate operacional do executável distribuído; chamadas reais de
cliente ainda dependem de Codex/Claude configurados.
