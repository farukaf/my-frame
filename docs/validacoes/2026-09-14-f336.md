# F336 — frescor da cobertura do Warframe Market

`get_source_coverage("warframe-market")` agora inclui `observedAt` nos campos conhecidos:
quotes usam a cotação mais recente, enquanto conta/ordens e índice usam seus respectivos
`RetrievedAt`. Campos `NotObserved` permanecem sem timestamp.

Validação:

```text
MyFrame.Mcp.Tests: 31 passed, 0 failed, 0 skipped
```
