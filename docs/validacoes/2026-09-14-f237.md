# F237 — smoke real Codex/Claude contra MCP 0.0.15

## Configuração

Os dois clientes foram atualizados para:

`artifacts/MyFrame-0.0.15-win-x64/MyFrame.Mcp.exe`

Ambos reportaram o servidor `my-frame` como conectado.

## Consultas

Cada cliente recebeu a instrução de chamar uma única vez
`get_capture_inbox_status`, sem executar comandos locais.

Os dois retornaram o mesmo contrato sanitizado:

```json
{"state":"not_initialized","pendingMarkers":0,"overwolfRunning":true,"warframeRunning":true,"heartbeatState":null,"heartbeatFresh":false,"validMarkers":0,"invalidMarkers":0}
```

O resultado confirma transporte, descoberta da ferramenta, execução stdio e
projeção dos campos novos em clientes reais. Não é evidência de captura F1:
heartbeat e marcadores continuam ausentes porque a extensão unpacked não foi
carregada no Overwolf.
