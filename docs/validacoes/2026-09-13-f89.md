# F89 — leitura concorrente app/MCP

F89 adiciona ao teste stdio uma leitura concorrente que simula a UI/app lendo o
SQLite enquanto o MCP consulta `get_activity`. Dez leituras locais da projeção
World State e a consulta MCP compartilham a mesma base temporária em WAL; ambas
retornam a bounty esperada sem escrita concorrente ou lock exposto.

Validação executada:

- Core: 148/148;
- MCP: 13/13;
- App Windows build sem warnings ou erros.
