# F83 — procedência no resultado do sync

F83 inclui `parserVersion` na mensagem imediata exibida após a sincronização do
World State. Um sync publicado informa explicitamente se usou o parser oficial ou
o fallback comunitário; falha continua mostrando o erro sanitizado. A informação
é apenas de transparência e não altera os dados publicados.

Validação executada:

- App Windows build Debug sem warnings ou erros;
- Core: 146/146;
- MCP: 13/13;
- working tree limpo após o commit.
