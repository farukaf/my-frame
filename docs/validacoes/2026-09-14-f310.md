# Validação F310 — cobertura do Warframe Market

`get_source_coverage` agora aceita `sourceId=warframe-market`, alinhando a
consulta ao conjunto de fontes registradas no status (`warframe-market`).

O teste MCP verifica que uma raiz sem sincronização retorna `state=
not_initialized`, sem inventar revisão, parser ou cotações. Quando houver uma
revisão publicada, o mesmo caminho retorna a cobertura e procedência já
persistidas.
