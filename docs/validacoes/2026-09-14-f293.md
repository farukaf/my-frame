# F293 — distribuição 0.0.27 e revalidação dos gates

## Distribuição

O pacote MyFrame-0.0.27-win-x64 foi construído com F285–F292.

## Gate offline

O fluxo snapshot Public Export → Sync → SQLite → MCP continuou aprovado:

    importState=published
    importedRecords=1
    mcpState=published
    mcpItems=1
    localizedNames=Known
    technicalMetadata=Known

## Preflight externo

Overwolf e Warframe estavam em execução, mas a extensão não estava carregada:

    OVERWOLF_COLLECTOR_READY=0
    state=missing
    heartbeatFresh=false
    validMarkers=0

O preflight emitiu a ação manual e o caminho da extensão; não houve tentativa de
bypass da interface do Overwolf.
