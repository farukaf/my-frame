# F326 — contrato textual de contexto e diferenças

Documentação do MCP, descrições das ferramentas e skill de farm agora explicam que o histórico
pode trazer `contextId` e que as diferenças cobrem equipamento, configuração, mods/upgrades e
quantidades. A skill exige usar o contexto quando presente e não inventar um quando ausente.

Validação:

- `scripts/Test-SkillsContract.ps1`: `SKILLS_CONTRACT_OK=1`.
- O texto de `docs/MCP.md` e as descrições de `get_inventory_history`/`get_inventory_changes`
  foram alinhados ao DTO implementado.
