# F262 — distribuição 0.0.22 com comparação de inventário

A distribuição `MyFrame-0.0.22-win-x64` inclui `get_inventory_changes`.

Validações:

- upgrade side-by-side 0.0.21 → 0.0.22: `DISTRIBUTION_UPGRADE_OK=1`;
- smoke read-only: `MCP_READONLY_OK=1`, `MCP_NETWORK_CONNECTIONS=0`, `MCP_EXIT=0`;
- Codex e Claude atualizados para o executável 0.0.22 e reportados como conectados.
