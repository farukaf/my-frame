# F118 — UX de credencial WFM

F118 adiciona à tela Settings:

- campo de entrada protegido (password entry) para token transitório;
- salvar pelo `MarketCredentialService`/DPAPI;
- status sem mostrar o valor do segredo;
- revogação local;
- limpeza do campo depois de salvar, rejeitar ou revogar.

O App compilou com sucesso. O fluxo não envia o token ao MCP, SQLite ou logs.
Login/renovação remota continuam pendentes porque dependem de um endpoint e
política oficial do Warframe.Market.
