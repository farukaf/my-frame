# Validação F144 — busca local no Public Export pelo MCP

Foi adicionada `search_public_export`, consulta read-only ao catálogo sincronizado
no SQLite. A busca aceita `uniqueName`, nome, alias e categoria, pagina por limite
de até 200 resultados e não acessa a rede nem devolve `raw_json`.

Validação:

```text
MyFrame.Mcp.Tests: 18/18
Ferramentas MCP descobertas: 21
MyFrame.App build: 0 erros, 0 warnings
```

A ferramenta retorna lista vazia quando não há revisão Public Export publicada;
isso é distinto de uma busca com dados conhecidos e evita depender do catálogo
AlecaFrame.
