# Validação F209 — avaliação F10 executável no Windows PowerShell

O verificador F10 foi ajustado para materializar o array JSON antes da contagem.
Isso evita que o Windows PowerShell trate os três casos como um único objeto,
mantendo o comportamento compatível com PowerShell 7.

## Evidências

```text
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Test-F10Evaluation.ps1
CASES_OK=3
RESULTS=NOT_PROVIDED
```

O comando valida a matriz oficial sem fingir resultados de modelo. A avaliação
antes/depois continua exigindo arquivos de resultado com revisão, cobertura,
fontes, chamadas MCP, latência e falhas críticas conforme o protocolo.

## Critérios

- [x] Três casos F10 reconhecidos no Windows PowerShell.
- [x] Compatibilidade preservada para PowerShell 7.
- [x] Nenhum resultado de LLM inventado pelo verificador.
- [ ] Execução completa antes/depois com clientes reais continua pendente.
