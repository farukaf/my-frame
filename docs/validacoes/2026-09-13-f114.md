# F114 — settings no SQLite

F114 adiciona `SqliteSettingsStore` e conecta App/MCP ao mesmo `data.db` usado
pelos caches de mercado. O `settings.v1.json` legado é importado apenas quando
a tabela está vazia e permanece intacto como rollback; o token do Warframe.Market
continua separado e nunca é persistido no banco.

Evidências:

```text
SqliteSettingsStoreTests: 1 passed, 0 failed
Core/MCP/App build: 0 warnings, 0 errors
MCP suite: 18 passed, 0 failed
MCP Inspector: 14 tools, 0 warnings, 0 errors
Inspector calls: 3/3 structured
```
