# Validação F304 — regressão sequencial da solução

Foi executado `scripts/Test-SolutionRegression.ps1` em Debug, com um projeto
por vez para evitar concorrência de build/teste.

Resultado:

```text
MyFrame.Core.Tests: 190/190
MyFrame.Mcp.Tests: 26/26
SOLUTION_REGRESSION_OK=1
SOLUTION_REGRESSION_PROJECTS=2
SOLUTION_REGRESSION_PARALLELISM=sequential
```

Os avisos NU1900 refletem indisponibilidade dos feeds de vulnerabilidade no
ambiente e não produziram erro de compilação ou teste. O baseline de avaliação
LLM F10 continua ausente conforme F303; esta regressão não o substitui.
