# Validação F177 — prontidão dos clientes MCP

## Execução

```powershell
pwsh -NoProfile -File .\scripts\Test-McpClientReadiness.ps1
```

Resultado:

```text
MCP_CLIENT_READINESS_PROBE_OK=1
MCP_CODEX_STATE=ok
MCP_CLAUDE_STATE=not-configured
MCP_CLIENT_READINESS_MUTATIONS=0
```

O probe foi somente leitura. O Codex possui uma entrada `my-frame` registrada;
o Claude CLI está instalado, mas ainda não possui configuração para esse
servidor. Nenhuma configuração, credencial ou arquivo externo foi alterado.

## Critério

- [x] Detectar os dois clientes sem mutação.
- [x] Confirmar prontidão da configuração Codex.
- [ ] Cadastrar `my-frame` no Claude e executar smoke real.
- [ ] Executar avaliação LLM antes/depois com os casos F10 oficiais.
