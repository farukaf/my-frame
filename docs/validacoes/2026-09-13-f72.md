# F72 — identificador do mercado no sync status

F72 corrige a divergência entre `SyncStatusReader`/UI (`warframe-market`) e o
MCP (`market-public`). O MCP agora consulta a mesma fonte persistida e não
classifica uma execução válida como `not_initialized` apenas por usar outro
identificador.

## Validação

- build MCP sem warnings;
- MCP 13/13;
- resposta stdio de `get_sync_status` contém `warframe-market`;
- nenhuma migração de dados é necessária: o identificador persistido já era o da UI/Core.
