# Validação F142 — cobertura do Public Export

Cada publicação do catálogo agora atualiza a tabela `coverage` para a fonte
`public-export`, distinguindo `Known` e `NotObserved` para `uniqueName`, `name`,
`aliases`, `category`, `description` e `rawJson`. A atualização ocorre na mesma
transação da revisão; uma revisão nova não herda cobertura antiga.

Validação:

```text
MyFrame.Core.Tests: 162/162
MyFrame.App build: 0 erros, 0 warnings
```

O teste de integração publica um fixture com aliases e sem categoria, e confirma
`aliases=Known` e `category=NotObserved`; nenhum campo ausente é transformado em
zero ou em categoria inventada.
