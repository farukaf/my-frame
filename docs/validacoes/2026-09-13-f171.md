# Validação F171 — avaliador F10 vinculado à matriz oficial

O script `Test-F10Evaluation.ps1` agora rejeita transcripts cujo `caseId` não
existe em `docs/avaliacao/f10-cases.json` e chamadas MCP sem nome. Isso evita
que um resultado sintaticamente completo mas fora da matriz seja contado como
baseline válido.

## Evidência

```text
CASES_OK=3
RESULTS=NOT_PROVIDED
```

O comando estrutural continua sem declarar qualidade de resposta; resultados
reais ainda exigem snapshot, revisão, cobertura, skills, modelo e avaliação
humana conforme o protocolo F10.
