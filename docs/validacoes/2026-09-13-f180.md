# Validação F180 — cadastro real Codex/Claude e verificador corrigido

## Cadastro

O MCP publicado foi registrado nos dois clientes:

```text
C:\...\artifacts\MyFrame-0.0.6-win-x64\MyFrame.Mcp.exe
```

Ambos os comandos de consulta reportaram o servidor conectado e habilitado.

## Verificação

```powershell
pwsh -NoProfile -File .\scripts\Test-McpClientPath.ps1 \
  -ServerPath .\artifacts\MyFrame-0.0.6-win-x64\MyFrame.Mcp.exe
pwsh -NoProfile -File .\scripts\Test-McpClientConfiguration.ps1 \
  -ServerPath .\artifacts\MyFrame-0.0.6-win-x64\MyFrame.Mcp.exe
```

Após corrigir o estado fixo do verificador, o resultado esperado é:

```text
MCP_CLIENT_PATH_PROBE_OK=1
MCP_CODEX_PATH_STATE=path-match
MCP_CLAUDE_PATH_STATE=path-match
MCP_CLIENT_PATH_MUTATIONS=0
MCP_CLIENT_CONFIG_OK=1
MCP_CODEX_CONFIGURED=1
MCP_CLAUDE_CONFIGURED=1
```

## Critério

- [x] Codex registrado no executável distribuído.
- [x] Claude registrado no executável distribuído.
- [x] Caminho verificado nos dois clientes.
- [x] Verificador não reporta Claude como falso negativo.
- [x] Claude executou `get_capabilities` em modo não interativo; chamada concluída sem negações.
- [x] Codex executou `get_capabilities` em modo não interativo; chamada MCP concluída.
- [ ] Executar os casos F10 completos através de clientes LLM reais.

## Evidência dos smokes reais

Os dois clientes receberam a mesma resposta estruturada, com sete capacidades,
incluindo `worldstate=available`, `market.public=available` e
`inventory.overwolf=pending_external_validation`. O Codex registrou a chamada
`my-frame/get_capabilities` como concluída; o Claude terminou com resultado
`success` e nenhuma negação de permissão.
