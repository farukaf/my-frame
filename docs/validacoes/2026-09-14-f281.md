# F281 — distribuição 0.0.26 e smoke do catálogo rico

## Objetivo

Validar o executável distribuído após F280 no fluxo local completo, sem depender
do registro de clientes externos.

## Execução

- distribuição construída em artifacts/MyFrame-0.0.26-win-x64;
- MyFrame.Sync.exe e MyFrame.Mcp.exe 0.0.26 executados diretamente;
- snapshot local F279 importado em uma raiz temporária;
- chamada MCP search_public_export feita por JSON-RPC stdio.

## Resultado

    importState       : published
    importedRecords   : 1
    mcpState          : published
    mcpItems          : 1
    itemName          : Test Weapon
    localizedNames    : Known
    technicalMetadata : Known
    activeRevision    : <revisão SQLite>

O controle de segurança não autorizou alterar novamente as configurações globais
do Codex/Claude nesta fase; por isso esta evidência usa o mesmo executável e
protocolo MCP diretamente, sem mutação fora do repositório.
