# F229 — tentativa de homologação F1 com processos reais ativos

## Evidência observada

Com o pacote reconstruído em `artifacts/collector-overwolf`:

- `OVERWOLF_PROCESS=1`;
- `WARFRAME_PROCESS=1`;
- `COLLECTOR_MANIFEST=1`;
- `COLLECTOR_EXTENSION_LOGGED=0`;
- `COLLECTOR_HEARTBEAT=0`;
- `CAPTURE_MARKERS=0`;
- `OVERWOLF_COLLECTOR_READY=0`.

O probe CLI confirmou `overwolfRunning=true` e `warframeRunning=true`, mas
`state=missing`, sem inbox, heartbeat ou marcador.

## Resultado

O gate F1 continua pendente. A extensão unpacked ainda precisa ser carregada
em **Overwolf → Development options → Load unpacked extension**, iniciada e
autorizada para exportar uma captura. Não foi fabricado dado sintético para
promover o gate.
