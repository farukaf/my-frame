# F266 — distribuição 0.0.23 com probe sanitizado

A distribuição `MyFrame-0.0.23-win-x64` incorpora o `MyFrame.Sync` da F265.

Validações:

- três executáveis publicados: App, MCP e Sync;
- probe empacotado: `state=unreachable`, `errorCode=PUBLIC_EXPORT_NETWORK_UNAVAILABLE`, exit 1;
- upgrade ativo 0.0.22 → 0.0.23 preservou o SQLite:
  `MCP_ACTIVE_UPGRADE_OK=1`;
- gate read-only: `MCP_READONLY_OK=1`, `MCP_NETWORK_CONNECTIONS=0`, `MCP_EXIT=0`.
