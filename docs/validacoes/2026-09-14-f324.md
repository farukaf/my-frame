# F324 — alterações de configuração nas diferenças MCP

`get_inventory_changes` agora compara também `configJson`, além de tipo, rank e estados de
cobertura. Assim, uma troca de configuração/mod que não altera o rank aparece como mudança do
equipamento, sem devolver o JSON privado.

Validação:

- teste MCP com uma entidade cuja única alteração é `configJson`;
- teste Core: 190 aprovados;
- build MCP: aprovado; avisos NU1900 são apenas indisponibilidade dos feeds de auditoria.
- a suíte MCP completa continua pendente neste ambiente por falha do resolver de workloads.

Atualização F327/F329: a suíte foi restaurada e executada; a contagem vigente é 31 testes
aprovados, 0 falhas e 0 skipped.
