# Validação F305 — coletor pronto para homologação F1

O pacote `MyFrame.Collector.Overwolf` foi revalidado com os testes Node:

```text
tests 15
pass 14
fail 0
skipped 1
```

O único caso omitido é o teste producer→probe .NET, pois o runner sandbox não
permite criação de subprocessos (`runner denies child-process creation`). A
suíte registra explicitamente esse motivo; a execução local elevada desse caso
deve ser feita no ambiente Windows de homologação.

O preflight externo continua:

```text
PACKAGE_VALID=1
OVERWOLF_PROCESS=1
WARFRAME_PROCESS=1
COLLECTOR_EXTENSION_LOGGED=0
COLLECTOR_HEARTBEAT=0
CAPTURE_MARKERS=0
OVERWOLF_ACTION=load-unpacked-extension
```

Portanto o pacote e os contratos estão prontos, mas F1 ainda não é declarada
concluída sem carregar a extensão e gerar um evento real no Warframe.
