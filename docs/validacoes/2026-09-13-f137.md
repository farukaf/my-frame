# Validação F137 — orquestrador Public Export compartilhado

## Alteração

`PublicExportSyncRunner` passou a concentrar a seleção do documento, decoder
LZMA-Alone, publicação no `SyncHost`, leitura de status e códigos de erro. O App
e `MyFrame.Sync` usam a mesma implementação; não há duas políticas de seleção
divergentes.

## Verificação

```text
MyFrame.Core.Tests: 161/161
MyFrame.App build: 0 erros, 0 warnings
MyFrame.Sync build: 0 erros, 0 warnings
```

O teste de integração usa um índice comprimido no mesmo formato da fonte oficial,
documento JSON, publicação em SQLite temporário e confirma caminho/tag/alias
localizado. Nenhuma rede é usada pelo teste.
