# F82 — procedência do parser na UI

F82 adiciona a versão do parser à página visual de status de sincronização. Cada
fonte mostra `Parser: ...` junto da revisão ativa; uma fonte sem revisão mostra
`Parser: —`. A UI continua somente leitura e consome o mesmo `SyncDatabase` usado
pelo MCP.

Validação executada:

- modelo `SyncSourceStatusRow` preserva parser oficial/comunitário e ausência;
- App Windows build Debug sem warnings ou erros;
- Core: 146/146;
- MCP: 13/13.
