# F249 — distribuição 0.0.18 com SyncHost de referências

A distribuição `MyFrame-0.0.18-win-x64` foi publicada com o importador de URLs
permitidas para Wiki/Overframe.

Validações:

- upgrade side-by-side 0.0.17 → 0.0.18: `DISTRIBUTION_UPGRADE_OK=1`;
- smoke read-only do MCP: `MCP_READONLY_OK=1`, `MCP_NETWORK_CONNECTIONS=0`,
  `MCP_EXIT=0`;
- Codex e Claude atualizados para o executável 0.0.18 e reportados como conectados.
