# Validação F188 — pacote 0.0.8 com contrato MCP completo

## Publicação

```powershell
pwsh -NoProfile -File .\scripts\Build-Distribution.ps1 `
  -Version 0.0.8 -Configuration Release -RuntimeIdentifier win-x64
```

Resultado: exit code 0.

## `tools/list` do executável publicado

O Inspector real retornou:

```text
COUNT=23
```

As seis ferramentas que faltavam no pacote anterior agora estão presentes:
`get_sync_history`, `get_inventory_coverage`, `get_source_coverage`,
`get_equipment`, `get_mods` e `get_loadout`.

Os restores emitiram somente avisos `NU1900` por indisponibilidade dos feeds de
auditoria NuGet.

## Critério

- [x] Publicar App, MCP e SyncHost 0.0.8.
- [x] Confirmar 23 ferramentas no protocolo MCP distribuído.
- [x] Confirmar ferramentas de inventário/cobertura disponíveis para clientes.
- [ ] Repetir smoke completo com captura e World State no mesmo root usando 0.0.8.
