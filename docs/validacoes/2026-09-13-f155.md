# Validação F155 — regressão após os fluxos locais

O runner sequencial `scripts/Test-SolutionRegression.ps1` foi executado após as
fases F151–F154, incluindo sincronização Public Export por arquivo/diretório,
World State por arquivo e `--all-local`.

Resultado:

```text
MyFrame.Core.Tests: 165 passed, 0 failed
MyFrame.Mcp.Tests: 18 passed, 0 failed
SOLUTION_REGRESSION_OK=1
SOLUTION_REGRESSION_PROJECTS=2
SOLUTION_REGRESSION_PARALLELISM=sequential
```

Os avisos NU1900 referem-se à consulta de vulnerabilidades sem acesso ao feed
NuGet e não alteraram o resultado dos testes.
