# F328 — distribuição 0.0.31

O pacote Windows foi publicado em `artifacts/MyFrame-0.0.31-win-x64` após as alterações de
contexto e histórico de inventário.

Gates executados:

```text
MCP_READONLY_OK=1
MCP_NETWORK_CONNECTIONS=0
MCP_EXIT=0
DISTRIBUTION_UPGRADE_OK=1
DISTRIBUTION_UPGRADE_MODE=side-by-side
OLD=0.0.30  NEW=0.0.31
```

O MCP não criou arquivos no data root limpo, não abriu sockets e encerrou corretamente após EOF.
