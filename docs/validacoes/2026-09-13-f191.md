# Validação F191 — snapshot combinado de inventário e Public Export

## Publicação

No mesmo data root temporário foram executados os executáveis publicados
0.0.8:

```text
--overwolf-inventory-directory: published, 1 capture, 1 equipamento, 1 stackable
--public-export-directory: published, 2 registros
```

## MCP

`get_overview` retornou:

- inventário `valid`, vindo de `SYNC_DATABASE`;
- catálogo `partial`, com `PUBLIC_EXPORT_MINIMAL`;
- `snapshotId` e `inventoryCapturedAt` presentes;
- aviso `SYNC_DATABASE_PARTIAL` informando que componentes e progressão não
  foram observados.

`search_inventory(text="Mesa Prime")` também retornou `structuredContent` com
o mesmo vetor de fontes e aviso, sem transformar catálogo parcial em certeza.

## Critério

- [x] Captura e catálogo publicados no mesmo SQLite.
- [x] Snapshot MCP identifica as revisões/fontes.
- [x] Cobertura parcial é exposta explicitamente.
- [x] Busca de inventário preserva o aviso de lacuna.
- [ ] Completar componentes, preços e ordens para o caso econômico F10.
