# F232 — regressão completa após o status de runtime

## Validação

- `MyFrame.Core.Tests`: 184 aprovados;
- `MyFrame.Mcp.Tests`: 23 aprovados;
- `SOLUTION_REGRESSION_OK=1`;
- execução sequencial dos dois projetos;
- App Windows `net10.0-windows10.0.19041.0/win-x64`: build aprovado;
- avisos NU1900 apenas pela indisponibilidade do proxy/NuGet.

Esta regressão não altera o resultado F1: a extensão ainda precisa ser
carregada no Overwolf para produzir heartbeat e captura reais.
