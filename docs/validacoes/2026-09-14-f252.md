# F252 — skills com detalhe de referência atribuído

As skills agora orientam a sequência `search_references` →
`get_reference_section`, preservando URL, revisão, autoria/licença e o limite de
conteúdo sem permitir fetch arbitrário pelo MCP. `warframe-research` passou para
versão 2; a skill de builds mantém a versão 4.

Validação: `SKILLS_CONTRACT_OK=1`.
