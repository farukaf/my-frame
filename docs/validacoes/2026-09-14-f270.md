# F270 — smoke econômico usa o pacote atual

`Test-EconomicInspector.ps1` agora seleciona automaticamente a distribuição
`MyFrame-*-win-x64` mais recente quando os caminhos não são informados. Isso
evita que o cenário de vendas/excedentes valide o MCP histórico 0.0.8.

Caminhos explícitos continuam suportados para regressão de versões antigas.
