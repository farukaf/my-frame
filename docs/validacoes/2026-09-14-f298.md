# Validação F298 — distribuição 0.0.28

## Artefato

Foi gerada a distribuição Windows x64:

```text
artifacts/MyFrame-0.0.28-win-x64
```

Ela inclui o hardening F297 do World State e o restante do MCP/App
publicável.

## Gates executados

- `Test-McpReadOnly.ps1`: `MCP_READONLY_OK=1`, `MCP_NETWORK_CONNECTIONS=0`,
  saída padrão vazia antes do protocolo e raiz limpa.
- `Test-DistributionUpgrade.ps1`: `DISTRIBUTION_UPGRADE_OK=1`, upgrade
  lado a lado de 0.0.27 para 0.0.28, ambos encerrando corretamente após EOF.
- Build de publicação App/Core/MCP/Sync: concluído.

Warnings XAML de otimização de binding e avisos de indisponibilidade do feed
de vulnerabilidades NuGet não produziram erros e não alteraram o gate
read-only. A homologação Overwolf/F1 continua separada e ainda requer carregar
manualmente a extensão e gerar eventos no Warframe.
