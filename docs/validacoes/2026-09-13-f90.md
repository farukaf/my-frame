# F90 — skill de farm com procedência de fonte

F90 atualiza `skills/warframe-farm` para consultar capacidades e status antes do
World State, registrar revisão/parser e identificar explicitamente o fallback
comunitário. A skill continua exigindo recompensa Mother Token explícita e não
calcula tokens/hora a partir de chance ou pool.

Validação executada:

- frontmatter conferido contra as propriedades aceitas pelo `quick_validate.py`;
  execução direta do validator ficou indisponível porque o ambiente não possui
  `PyYAML` e o índice de pacotes não está acessível;
- contrato documentado para `get_capabilities`, `get_sync_status`, `get_activity`
  e `coverage.motherTokens`;
- nenhuma fonte externa ou lista de meta foi embutida na skill.
