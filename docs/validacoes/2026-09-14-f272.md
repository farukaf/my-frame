# F272 — smoke real Codex/Claude no MCP 0.0.24

Com ambos os clientes apontando para `MyFrame-0.0.24-win-x64`, cada um chamou
uma única vez `get_capture_inbox_status` em modo read-only.

Resultado convergente:

```json
{"state":"not_initialized","heartbeatFresh":false}
```

O Codex executou a ferramenta diretamente. No Claude, a ferramenta read-only
foi explicitamente permitida para o modo não interativo; não houve negação nem
uso de shell. O resultado confirma transporte, descoberta e contrato, mas não
comprova F1: a extensão ainda não foi carregada no Overwolf.
