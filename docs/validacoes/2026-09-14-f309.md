# Validação F309 — distribuição 0.0.29

Foi gerada a distribuição Windows x64:

```text
artifacts/MyFrame-0.0.29-win-x64
```

O artefato inclui `worldStateParserVersion` no `get_acquisition` e a cobertura
de testes F308.

Gates executados:

- `Test-McpReadOnly.ps1`: `MCP_READONLY_OK=1`, zero conexões e raiz limpa;
- `Test-DistributionUpgrade.ps1`: `DISTRIBUTION_UPGRADE_OK=1`, upgrade
  lado a lado de 0.0.28 para 0.0.29;
- publicação App/Core/MCP/Sync concluída.

Warnings XAML e NU1900 dos feeds de vulnerabilidade não produziram erros. A
captura real Overwolf continua um gate externo separado.
