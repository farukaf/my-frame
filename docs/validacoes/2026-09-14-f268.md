# F268 — distribuição 0.0.24 com ingestão de referências sanitizada

A distribuição `MyFrame-0.0.24-win-x64` inclui a correção F267 no `MyFrame.Sync`.

Validações:

- referência empacotada: `REFERENCE_NETWORK_UNAVAILABLE`, exit 1 e nenhum arquivo gravado;
- upgrade ativo 0.0.23 → 0.0.24: `MCP_ACTIVE_UPGRADE_OK=1` e SQLite preservado;
- gate read-only: `MCP_READONLY_OK=1`, `MCP_NETWORK_CONNECTIONS=0`, `MCP_EXIT=0`.
