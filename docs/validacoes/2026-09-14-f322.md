# F322 — upgrades nas revisões e diferenças de inventário

## Escopo

As capturas já persistiam mods/upgrades na tabela `inventory_upgrades`, mas a leitura de uma
revisão (`GetInventoryRevisionDataAsync`) descartava esses registros. Agora a revisão retorna
os upgrades com `ownerInstanceId`, `sourceField`, `upgradeId` e `rank`; `get_inventory_changes`
também identifica adições, remoções e alterações de upgrades sem retornar payload bruto.

## Validação

- `dotnet test MyFrame.Core.Tests/MyFrame.Core.Tests.csproj --no-restore`: 190 aprovados, 0
  falhas.
- O teste `InventoryPublicationPreservesAttributedUpgrades` agora verifica persistência e
  re leitura na revisão SQLite.
- `dotnet build MyFrame.Mcp/MyFrame.Mcp.csproj --no-restore`: compilação aprovada; avisos
  NU1900 referem-se apenas à indisponibilidade dos feeds de auditoria NuGet.
- A suíte MCP foi iniciada, mas não concluiu neste ambiente; não há evidência para declarar
  sua execução verde nesta fase.
