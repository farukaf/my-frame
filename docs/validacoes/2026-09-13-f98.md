# F98 — interoperabilidade local do coletor e probe

F98 fecha a validação local do framing producer → probe sem afirmar captura real
do GEP. O teste Node do coletor agora marca explicitamente como `skip` o caso em
que o runner bloqueia criação de processos (`EPERM`); em um Windows autorizado,
ele continua executando o `.exe` normalmente.

Validação executada:

- Node: 14 aprovados, 1 skip documentado por restrição do runner;
- probe .NET executado diretamente sobre marker produzido pelo coletor;
- probe retornou `gameId=8954`, `payloadRootObject=true`, `completeness=unverified`
  e `publishable=false`;
- nenhum valor de inventário foi impresso pelo relatório estrutural;
- pacote Overwolf reconstruído e verificado pelo preflight.

O gate F1 permanece aberto até carregar a extensão no Overwolf, abrir o Warframe e
observar eventos GEP reais com comparação manual do inventário.
