# F288 — resolução canônica e filtro técnico do catálogo

As consultas Public Export agora:

- aceitam productCategory no filtro category;
- resolvem uniqueName, nome e alias por identidade canônica, tolerando
  diferenças de acentos e espaços;
- continuam comparando sem rede e sem alterar a revisão SQLite.

O teste MCP confirma filtro LongGuns e consulta pelo alias localizado.
