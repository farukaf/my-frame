# F0 — baseline e validação

Data: 12/09/2026. Estado: aceite local da F0 aprovado; revisão/merge do PR são etapas
separadas. Base imutável: `d35eb1fd5733742265b4779b7e9b7eef5ab15f48`
(`feat/local-mcp`). Branch de entrega: `feat/data-platform-f0`.
PR: [#6](https://github.com/farukaf/my-frame/pull/6), criado por `gh stack submit`.

## Ambiente e isolamento

- Windows 10.0.26200, win-x64; SDK .NET 10.0.401; runtime .NET 10.0.12.
- Workload `maui-windows` 10.0.20; MSBuild 18.9.11.
- ModelContextProtocol 2.2.0; xUnit 2.9.3; Microsoft.NET.Test.Sdk 17.14.1.
- Node 24.11.1 para exemplo de consumo; GitHub CLI 2.92.0 e `gh-stack` 0.1.1.
- Contratos internos de settings/regras/MCP permanecem na versão 1. Protocolo
  negociado pelo SDK é registrado no output do teste stdio; não confundir com DTO v1.
- Worktree isolado em `artifacts/f0-validation`, inicialmente no commit base.
  As alterações preexistentes de `DashboardViewModel.cs` e `Platforms/Windows/App.xaml.cs`
  não fazem parte da F0; aplicativo/MCP abertos não foram encerrados.

Não usamos dados reais do jogo. Testes stdio definem `MYFRAME_DATA_ROOT` para
diretório temporário vazio e verificam que o servidor não criou arquivos nele.
Isso não substitui a prova abrangente de ausência de escrita/rede prevista em F8.

## Baseline executada antes das mudanças

Comandos dentro do worktree no commit base:

```powershell
dotnet build MyFrame.slnx -m:1 -nr:false
dotnet test MyFrame.Core.Tests/MyFrame.Core.Tests.csproj --no-build --no-restore -m:1 --logger "trx;LogFileName=baseline-core.trx"
dotnet test MyFrame.Mcp.Tests/MyFrame.Mcp.Tests.csproj --no-build --no-restore -m:1 --logger "trx;LogFileName=baseline-mcp.trx"
./scripts/Build-Distribution.ps1 -Version 0.0.2
```

| Verificação | Resultado |
| --- | --- |
| Restore/build isolados | Aprovado, zero avisos/erros; build em 76,91 s. |
| Core anterior | 70 aprovados, zero falhas/ignorados. |
| MCP anterior | 11 aprovados, zero falhas/ignorados; inclui transporte stdio real. |
| Publicação self-contained anterior | Aprovada; app e MCP gerados em `artifacts/MyFrame-0.0.2-win-x64`. Avisos preexistentes XC0022/XC0025 de bindings XAML. |

Falhas de ambiente registradas: build na árvore original encontrou DLLs em uso
(MSB3027/MSB3021). Build de solução sem `-m:1` terminou sem diagnóstico útil;
execução serial e isolada passou. Publicação inicial foi bloqueada pelo proxy da
sandbox ao baixar Crossgen2 (NU1301); repetida com acesso permitido. O runner Node
com subprocesso encontrou `EPERM`; os oito casos passam com `--test-isolation=none`.
Não tratar essas ocorrências como regressões funcionais corrigidas no produto.

## Mudanças e testes da F0

- [Auditoria](../AUDITORIA-ALECAFRAME.md): fontes, composição, settings, mercado,
  watchers, contexto, migração e limitações do modelo atual.
- [Decisões abertas](../DECISOES-PLATAFORMA.md): nove decisões, capacidades mínimas
  e fluxo stacked PR com a base solicitada.
- Teste temporal: retenção não é renovada pela leitura; snapshot expira e orienta
  nova análise. Usa `TimeProvider` controlado, sem espera de cinco minutos.
- Teste stdio: erro `SNAPSHOT_EXPIRED` e setup ausente retornam `isError` com texto,
  sem conteúdo estruturado; exemplos não podem mascarar isso como lista vazia.
- Busca válida sem resultados preserva metadados e total zero.
- [Exemplo de consumidor](../../examples/mcp/README.md): oito casos sem dependências npm.
- Instruções do servidor e recurso de schema esclarecem erro e expiração; nenhuma
  alteração de schema de resposta, política de retenção ou fonte de dados nesta fase.

Validação do código de entrega no commit `346e936`, no mesmo worktree isolado:

| Verificação | Resultado |
| --- | --- |
| Build Debug da solução | Aprovado, zero avisos/erros, 29,42 s. |
| Core | 71 aprovados, zero falhas/ignorados. |
| MCP | 12 aprovados, zero falhas/ignorados. |
| Consumidor JavaScript | 8 aprovados, zero falhas/ignorados. |
| Protocolo negociado no teste stdio | `2026-07-28`, com SDK 2.2.0. |
| Links Markdown | 80 links locais/âncoras verificados na primeira revisão F0. |
| Publicação F0 + smoke do executável publicado | Aprovados; app/MCP self-contained gerados e 1 teste stdio aprovado contra o executável publicado, sem falhas/ignorados. Mesmos avisos XAML preexistentes. |

Comandos de repetição, após build:

```powershell
dotnet test MyFrame.Core.Tests/MyFrame.Core.Tests.csproj --no-build --no-restore -m:1 --logger "trx;LogFileName=f0-core.trx"
dotnet test MyFrame.Mcp.Tests/MyFrame.Mcp.Tests.csproj --no-build --no-restore -m:1 --logger "trx;LogFileName=f0-mcp.trx"
node --test --test-isolation=none examples/mcp/read-inventory-result.test.mjs
./scripts/Build-Distribution.ps1 -Version 0.0.3
$env:MYFRAME_MCP_TEST_SERVER = (Resolve-Path artifacts/MyFrame-0.0.3-win-x64/MyFrame.Mcp.exe).Path
dotnet test MyFrame.Mcp.Tests/MyFrame.Mcp.Tests.csproj --no-build --no-restore -m:1 --filter FullyQualifiedName~RealStdioServerListsAndCallsStructuredCapabilities --logger "trx;LogFileName=f0-published-mcp.trx"
```

TRX ficam nos `TestResults` de cada projeto do worktree, ignorados pelo Git. As
versões `0.0.2`/`0.0.3` identificam pastas locais do script de distribuição, não
uma release publicada nem alteração de versão interna dos assemblies. Metadados e
documentação finais podem ser acrescentados após `346e936` sem alterar código testado.

## Pendências que continuam abertas

O mapeamento de cada pendência antiga para novos testes está na
[matriz, seção 11](../VALIDACAO-PLATAFORMA.md#11-gates-e-transferência-das-pendências-atuais).
O teste temporal da F0 não encerra o gate de memória/concorrência da F8.
MCP Inspector, Codex/Claude reais, migração SQLite, paridade ampla e captura Overwolf
não foram homologados nesta fase. Aprovação de distribuição e acesso Wiki/Overframe
continuam hipóteses a provar nas fases próprias.
