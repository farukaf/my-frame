# F330 — distribuição 0.0.32

O pacote Windows `artifacts/MyFrame-0.0.32-win-x64` foi gerado com o isolamento de contexto
de inventário.

Gates:

```text
MCP_READONLY_OK=1
MCP_NETWORK_CONNECTIONS=0
MCP_EXIT=0
DISTRIBUTION_UPGRADE_OK=1
DISTRIBUTION_UPGRADE_MODE=side-by-side
OLD=0.0.31  NEW=0.0.32
```
