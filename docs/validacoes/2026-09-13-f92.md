# F92 — skill de economia com procedência do mercado

F92 atualiza `skills/warframe-economy` para consultar capacidades/status antes de
preços, registrar estado/revisão/parser de `warframe-market` e marcar cálculos
como parciais quando a fonte não estiver atual. A skill preserva preço ausente ou
stale como `null`/incerto e não cria ordens nem autentica.

Validação executada:

- frontmatter estrutural das três skills conferido;
- contrato comum e procedência do mercado documentados;
- `quick_validate.py` indisponível por PyYAML ausente no ambiente.
