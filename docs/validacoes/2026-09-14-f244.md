# F244 — teste do warning de captura não verificada

O teste `UsesSynchronizedDataWhenAlecaSettingsAreMissing` agora isola
`MYFRAME_DATA_ROOT` e verifica que um snapshot SQLite sem inbox Overwolf pronta
contém `INVENTORY_CAPTURE_UNVERIFIED`.

Validação: Core com 184 testes aprovados.
