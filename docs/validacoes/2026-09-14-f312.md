# Validação F312 — documentação de procedência MCP

`docs/MCP.md` agora descreve os campos de procedência adicionados nas fases
F300 e F307:

- `parserVersion` em `get_bounties`, `get_world_state` e `get_activity`;
- `worldStateParserVersion` em `get_acquisition`;
- revisão ativa associada às respostas.

Isso mantém o contrato escrito alinhado ao schema/runtime e orienta clientes a
atribuir corretamente endpoint oficial, fallback comunitário ou fixture.
