# Validação F161 — tentativa de homologação local Overwolf

Esta execução verificou o ambiente local para homologar a captura F1 com dados
reais, sem criar payload sintético nem alterar os dados do usuário.

## Evidência observada

Com Overwolf aberto e Warframe em execução:

```text
PACKAGE_BUILD=1
PACKAGE_VALID=1
OVERWOLF_PROCESS=1
WARFRAME_PROCESS=1
COLLECTOR_MANIFEST=1
COLLECTOR_EXTENSION_LOGGED=0
COLLECTOR_HEARTBEAT=0
COLLECTOR_RUNTIME_EVIDENCE=0
CAPTURE_MARKERS=0
OVERWOLF_COLLECTOR_READY=0
OVERWOLF_PREFLIGHT_READY=0
```

Os logs recentes do Overwolf contêm somente AlecaFrame e extensões internas; não
há renderer ou log associado ao pacote `artifacts/collector-overwolf`.

## Resultado

A homologação F1 permanece pendente. O pacote local foi construído e validado,
mas a extensão ainda não foi carregada pelo fluxo de desenvolvimento do
Overwolf. A sessão CUA disponível nesta execução não expôs uma janela/app do
Overwolf para automatizar esse passo.

## Próximo passo manual

1. No Overwolf, abrir **Development options → Load unpacked extension**.
2. Selecionar exatamente
   `C:\Users\faruk\source\repos\my-frame\artifacts\collector-overwolf`.
3. Abrir a UI **My Frame Collector Dev**, clicar em **Salvar relatório de
   estrutura** e confirmar a pasta de inbox sugerida.
4. Executar novamente:

   ```powershell
   pwsh -NoProfile -File .\scripts\Test-OverwolfCollectorReadiness.ps1
   ```

5. Considerar F1 comprovada somente quando `OVERWOLF_COLLECTOR_READY=1` e os
   marcadores `.ready.json` forem produzidos; então validar o conteúdo com
   `MyFrame.Collector.Probe` e registrar apenas metadados anonimizados.

Nenhum inventário, identificador de conta, token ou payload bruto foi
versionado nesta tentativa.
