# F323 — diferenças de mods no MCP

O teste MCP de diferenças agora publica duas revisões completas com o mesmo equipamento e
troca de mod. A resposta esperada inclui uma mudança `kind=upgrade`, preservando identidade e
rank, sem serializar o payload privado dos registros.

Validações executadas:

- `dotnet build MyFrame.Mcp/MyFrame.Mcp.csproj --no-restore`: aprovado.
- `dotnet test MyFrame.Core.Tests/MyFrame.Core.Tests.csproj --no-restore`: 190 aprovados.
- A compilação/execução isolada de `MyFrame.Mcp.Tests` continua sem conclusão neste ambiente,
  por falha anterior do resolver de workloads do SDK; o teste permanece pronto para CI.
