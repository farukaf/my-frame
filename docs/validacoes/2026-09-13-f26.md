# F26 — artefato distribuível Windows

Comandos executados:

```text
pwsh -File scripts/Build-Distribution.ps1 -Version 0.0.6 -Configuration Release -RuntimeIdentifier win-x64
pwsh -File scripts/Build-Collector.ps1 -IconPath MyFrame.Collector.Overwolf/icon.png
pwsh -File scripts/Test-CollectorPackage.ps1 -PackagePath artifacts/collector-overwolf
```

Artefato `artifacts/MyFrame-0.0.6-win-x64`:

| Arquivo | Bytes |
| --- | ---: |
| `MyFrame.App.exe` | 298.463.518 |
| `MyFrame.Mcp.exe` | 81.151.724 |
| `e_sqlite3.dll` | 1.978.880 |

Gates adicionais: Core 128/128, MCP 12/12, Overwolf Node 15/15 e preflight do
pacote do coletor aprovado. O publish terminou com exit 0, mas registra avisos
XC0022/XC0025 de bindings XAML não compilados; esses avisos ficam documentados
como otimização de performance pendente, não como falha funcional.
