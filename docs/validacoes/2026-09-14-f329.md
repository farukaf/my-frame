# F329 — isolamento de contexto em diferenças

`get_inventory_changes` agora retorna `context_mismatch` e nenhum item quando as duas revisões
possuem `contextId` não nulo e diferente. Isso impede comparar inventários de contas/sessões
distintas. Se um dos lados não tiver contexto, a comparação existente é preservada, sem inventar
identidade.

Validação:

```text
MyFrame.Mcp.Tests: 31 passed, 0 failed, 0 skipped
```

O teste isolado também foi executado antes da suíte completa e passou.
