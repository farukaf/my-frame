# F235 — distribuição 0.0.15 com status de runtime

## Validação

- `Build-Distribution.ps1 -Version 0.0.15 -RuntimeIdentifier win-x64`: concluído;
- artefatos `MyFrame.App.exe`, `MyFrame.Mcp.exe` e `MyFrame.Sync.exe` presentes;
- smoke side-by-side 0.0.14 → 0.0.15: `DISTRIBUTION_UPGRADE_OK=1`;
- processos MCP encerraram por EOF sem stdout indevido e sem escrita nas raízes
  limpas;
- warnings NU1900/XamlC são não bloqueantes e foram registrados no build.
