# F337 — distribuição 0.0.33

Pacote gerado: `artifacts/MyFrame-0.0.33-win-x64`.

Gates:

```text
MCP_READONLY_OK=1
MCP_NETWORK_CONNECTIONS=0
MCP_EXIT=0
DISTRIBUTION_UPGRADE_OK=1
DISTRIBUTION_UPGRADE_MODE=side-by-side
OLD=0.0.32  NEW=0.0.33
```
