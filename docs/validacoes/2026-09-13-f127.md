# Validação F127 — distribuição 0.0.8 e pacote Overwolf

Artefatos gerados:

```text
artifacts/MyFrame-0.0.8-win-x64/MyFrame.App.exe  298984519 bytes
artifacts/MyFrame-0.0.8-win-x64/MyFrame.Mcp.exe   81274092 bytes
```

Gates executados:

```text
Collector package valid
MCP_READONLY_OK=1
MCP_NETWORK_CONNECTIONS=0
MCP_EXIT=0
MCP_INSPECTOR_CALLS_OK=1
MCP_INSPECTOR_CALLS_COUNT=3
MCP_INSPECTOR_CALLS_STRUCTURED=3
```

O publish emitiu `NU1900` por indisponibilidade temporária dos índices de
vulnerabilidade NuGet e avisos XAML de bindings não compilados; não houve erro
de build/publicação. A homologação Overwolf ainda depende de carregar a
extensão e abrir o Warframe com GEP real.
