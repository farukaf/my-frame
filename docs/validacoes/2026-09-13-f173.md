# Validação F173 — smoke do MCP Inspector com credencial WFM

O smoke `Test-McpInspectorCalls.ps1` agora chama também
`get_market_credential_status`, além de capabilities, sync status e World State.
O resultado esperado passa a ser quatro respostas `structuredContent`, sem
retornar o segredo.

O script pode ser executado com `-SkipInstall` quando o Inspector já estiver
disponível localmente; a execução não inicia sincronização nem usa rede de jogo.
