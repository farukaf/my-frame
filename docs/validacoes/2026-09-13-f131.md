# Validação F131 — smoke da fonte oficial Public Export

O endpoint usado pelo sincronizador respondeu em leitura somente:

```text
PUBLIC_EXPORT_STATUS=200
PUBLIC_EXPORT_BYTES=490
```

O smoke confirma disponibilidade HTTP do índice binário `.lzma`; não grava
SQLite, não chama o App e não declara publicação de catálogo. O parse LZMA e a
publicação continuam cobertos pelos fixtures/testes locais e pelo botão manual
F130.
