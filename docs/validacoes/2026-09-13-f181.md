# Validação F181 — smoke real do caso Mother Token com fonte não inicializada

## Execução

O Claude, usando o MCP distribuído F175, recebeu a pergunta do caso oficial
`deimos-mother-token` e foi instruído a chamar somente `get_world_state`.

Resultado estruturado retornado pelo MCP:

```json
{
  "state": "not_initialized",
  "lastAttemptAt": null,
  "errorCode": null,
  "bounties": [],
  "cycles": [],
  "activeRevisionId": null
}
```

O cliente concluiu corretamente que a lista vazia não prova ausência de Mother
Token: a fonte ainda não foi populada. Não inventou taxa por hora, recompensa ou
validade de bounty.

## Interpretação

Este é um smoke real de comportamento seguro, não um baseline F10 aprovado. A
matriz completa continua exigindo snapshot semeado, revisão, fontes e avaliação
antes/depois nos três casos oficiais.

## Critério

- [x] Cliente LLM real usou o MCP distribuído.
- [x] Chamada `get_world_state` retornou estrutura válida.
- [x] Estado `not_initialized` foi distinguido de ausência de recompensa.
- [x] Nenhuma conclusão inventada foi observada.
- [ ] Popular World State real e executar baseline F10 completo.
