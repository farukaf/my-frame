# Validação F170 — baseline após F168/F169

Regressão executada na branch atual antes dos gates externos.

```text
MyFrame.Core.Tests: 171 passed, 0 failed
MyFrame.Mcp.Tests: 20 passed, 0 failed
Collector Node: 14 passed, 1 skipped (subprocesso bloqueado pelo sandbox)
F10 structural gate: CASES_OK=3, RESULTS=NOT_PROVIDED
```

O gate F10 continua sem baseline real de avaliação antes/depois. A captura
Overwolf, o acesso de rede oficial e smoke tests em clientes MCP reais seguem
como validações externas, não foram marcados como concluídos.
