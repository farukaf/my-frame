# F258 — distribuição 0.0.20 com cobertura de referências

A distribuição `MyFrame-0.0.20-win-x64` inclui a cobertura `references` no MCP.

Validações:

- upgrade side-by-side 0.0.19 → 0.0.20: `DISTRIBUTION_UPGRADE_OK=1`;
- smoke read-only: `MCP_READONLY_OK=1`, `MCP_NETWORK_CONNECTIONS=0`, `MCP_EXIT=0`;
- Codex e Claude atualizados para o executável 0.0.20 e reportados como conectados.
