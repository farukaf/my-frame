# F25 — execução do gate estrutural F10

O verificador da avaliação F10 foi executado no estado atual:

```text
pwsh -File scripts/Test-F10Evaluation.ps1
CASES_OK=3
RESULTS=NOT_PROVIDED
```

O contrato da matriz está íntegro para os três casos (`build-unknown-inventory`,
`deimos-mother-token` e `economy-partial-price`). Nenhum diretório de resultados
foi encontrado, portanto o verificador corretamente não fabricou um baseline.

O item de avaliação antes/depois continua aberto. Para concluí-lo ainda é
necessário executar as perguntas com um cliente LLM real, registrar chamadas
MCP/snapshot/fontes/cobertura e revisar falhas críticas com dados reais F1.
