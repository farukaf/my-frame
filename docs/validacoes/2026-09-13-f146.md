# Validação F146 — inbox sugerida no coletor Overwolf

Quando o Overwolf fornece `io.paths.localAppData`, a UI do coletor preenche a
pasta `%LOCALAPPDATA%\MyFrame\captures`, que é a inbox observada pelo App. A
exportação continua manual, exige pasta absoluta e o payload bruto continua
dependente do checkbox de consentimento.

Validação:

```text
Node collector tests: 14 passed, 1 skipped (EPERM do sandbox no subprocesso)
Build-Collector.ps1 -IconPath .\MyFrame.Collector.Overwolf\icon.png: aprovado
Test-CollectorPackage.ps1: pacote válido
```

Isto reduz erro operacional de caminho, mas não fecha F1: a extensão ainda
precisa ser carregada no Overwolf e produzir evento GEP real.
