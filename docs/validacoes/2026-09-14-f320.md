# Validação F320 — distribuição 0.0.30

Foi gerada a distribuição Windows x64:

```text
artifacts/MyFrame-0.0.30-win-x64
```

O artefato inclui a cobertura `warframe-market`, normalização canônica de
`sourceId` e as reconciliações F319.

Gates:

- `Test-McpReadOnly.ps1`: `MCP_READONLY_OK=1`, zero conexões e raiz limpa;
- `Test-DistributionUpgrade.ps1`: `DISTRIBUTION_UPGRADE_OK=1`, upgrade
  lado a lado de 0.0.29 para 0.0.30;
- publicação App/Core/MCP/Sync concluída.

Warnings XAML e NU1900 de feeds indisponíveis não produziram erros. F1 real e
baseline F10 continuam gates externos.
