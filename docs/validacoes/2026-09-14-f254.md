# F254 — distribuição 0.0.19 com detalhe de referências

A distribuição `MyFrame-0.0.19-win-x64` inclui `get_reference_section` e as
skills atualizadas.

Validações:

- upgrade side-by-side 0.0.18 → 0.0.19: `DISTRIBUTION_UPGRADE_OK=1`;
- smoke read-only: `MCP_READONLY_OK=1`, `MCP_NETWORK_CONNECTIONS=0`, `MCP_EXIT=0`;
- Codex e Claude atualizados para o executável 0.0.19 e reportados como conectados.
