# F260 — distribuição 0.0.21 com histórico de inventário

A distribuição `MyFrame-0.0.21-win-x64` inclui `get_inventory_history`.

Validações:

- upgrade side-by-side 0.0.20 → 0.0.21: `DISTRIBUTION_UPGRADE_OK=1`;
- smoke read-only: `MCP_READONLY_OK=1`, `MCP_NETWORK_CONNECTIONS=0`, `MCP_EXIT=0`;
- Codex e Claude atualizados para o executável 0.0.21 e reportados como conectados.
