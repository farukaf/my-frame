# F333 — regressão integrada

Regressão executada fora do sandbox restrito:

```text
Node collector: 15 pass, 0 fail, 0 skipped
Core: 190 pass, 0 fail, 0 skipped
MCP: 31 pass, 0 fail, 0 skipped
```

O caso producer → probe do coletor também passou nesta execução local. Isso certifica os contratos
sintéticos e a integração do código, mas não substitui a captura real no Overwolf (F1), que ainda
está sem heartbeat/markers.
