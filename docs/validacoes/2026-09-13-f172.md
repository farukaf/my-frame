# Validação F172 — build do MCP com status de credencial

O projeto distribuível do MCP foi compilado após F168/F171.

```text
MyFrame.Mcp: Build succeeded
Errors: 0
Warnings: 2 (NU1900, feeds de vulnerabilidade inacessíveis)
```

Não houve aviso de plataforma do armazenamento protegido; o fallback não
Windows é selecionado por `OperatingSystem.IsWindows()` durante testes.
