# F332 — contrato de contexto incompatível

O contrato textual do MCP e a skill de farm agora documentam que `get_inventory_changes` retorna
`context_mismatch` com lista vazia quando as duas revisões têm `contextId` conhecido e diferente.
Isso impede que a LLM combine progresso de contas/sessões distintas.

Validação:

- `Test-SkillsContract.ps1`: `SKILLS_CONTRACT_OK=1`.
- preflight F1 atual: Overwolf e Warframe em execução, mas `heartbeatFresh=false`, `validMarkers=0`
  e `OVERWOLF_ACTION=load-unpacked-extension`; portanto, a captura real continua pendente.
