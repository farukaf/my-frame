# Validação F193 — teste automatizado de identidade de mercado e pacote 0.0.9

## Cobertura automatizada

Foi adicionado o teste `SynchronizedReaderPreservesMarketIdentityForItemsAndComponents`.
Ele publica um registro Public Export no SQLite, lê o snapshot sincronizado e
confirma que o índice contém as identidades do item e dos componentes:

```text
Core: 172 passed, 0 failed
MCP (vstest, incluindo os três cenários stdio reais): 20 passed, 0 failed
```

Os três testes que iniciam servidores MCP stdio reais (`RealStdio...`,
`ActiveMcpServer...` e `TwoRealStdio...`) foram executados individualmente e
também dentro da suíte completa, sem falhas.

## Distribuição

O pacote `artifacts/MyFrame-0.0.9-win-x64` foi publicado com sucesso contendo
`MyFrame.App.exe`, `MyFrame.Mcp.exe` e `MyFrame.Sync.exe`.

O MCP Inspector validou o executável distribuído:

```text
MCP_INSPECTOR_OK=1
MCP_INSPECTOR_TOOLS=23
MCP_INSPECTOR_WARNINGS=0
MCP_INSPECTOR_ERRORS=0
```

## Critérios

- [x] Índice `marketId`/`marketSlug` coberto por teste unitário.
- [x] Core completo passou com 172 testes.
- [x] Pacote 0.0.9 contém os três executáveis obrigatórios.
- [x] Contrato MCP distribuído expõe 23 ferramentas sem warnings/errors.
- [x] Homologar os três testes de stdio no Windows.
