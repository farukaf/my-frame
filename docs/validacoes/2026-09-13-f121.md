# Validação F121 — matriz completa do contrato MCP

O gate `scripts/Test-McpContract.ps1 -NoBuild` foi executado contra a suíte
real `MyFrame.Mcp.Tests` após F120.

Resultado:

```text
Passed: 18, Failed: 0, Skipped: 0
MCP_CONTRACT_OK=1
MCP_CONTRACT_SCOPE=stdio,schemas,pagination,cursors,errors,context-switch,readonly
```

Os 18 testes cobrem schemas nullable portáteis, documentação versus ferramentas
registradas, snapshot/cursor vinculado à consulta, troca de dados durante uma
paginação, ausência de setup, enums/limites, fila/timeout, stdio estruturado,
reabertura de banco migrado, dois servidores concorrentes e fixture de 20.000
itens.
