# F285 — diagnóstico de callbacks no MCP

O contrato de get_capture_inbox_status foi ampliado com sinais estruturais do
heartbeat do coletor:

- collectorState;
- supportedFeatures;
- eventCounts;
- lastEventFeature;
- lastEventAt.

Os campos são opcionais para compatibilidade com heartbeats antigos e nunca
incluem payload, username, caminho local ou valores do inventário.

Validação:

    CollectorCaptureStatusProbeTests: 3/3
    MyFrame.Mcp.Tests: 25/25
