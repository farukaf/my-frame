name: PR artifact comment

on:
  workflow_run:
    workflows:
      - CI/CD
    types:
      - completed

permissions:
  actions: read
  issues: write
  pull-requests: read

jobs:
  comment:
    name: Publicar link do instalador no PR
    if: >-
      github.event.workflow_run.event == 'pull_request' &&
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest

    steps:
      - name: Criar ou atualizar comentário
        uses: actions/github-script@v8
        env:
          RUN_ID: ${{ github.event.workflow_run.id }}
          RUN_URL: ${{ github.event.workflow_run.html_url }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const runId = Number(process.env.RUN_ID);
            const runUrl = process.env.RUN_URL;
            const headSha = process.env.HEAD_SHA;
            const marker = '<!-- myframe-pr-artifact -->';

            let pullRequest = context.payload.workflow_run.pull_requests?.[0];

            if (!pullRequest) {
              const { data: associatedPullRequests } =
                await github.rest.repos.listPullRequestsAssociatedWithCommit({
                  owner,
                  repo,
                  commit_sha: headSha,
                });

              pullRequest = associatedPullRequests.find(candidate =>
                candidate.state === 'open' &&
                candidate.base.repo.full_name === `${owner}/${repo}`
              );
            }

            if (!pullRequest) {
              core.warning(`Nenhum PR aberto foi encontrado para ${headSha}.`);
              return;
            }

            const pullNumber = Number(pullRequest.number);
            const artifactName = `MyFrame-pr-${pullNumber}-win-x64`;
            const { data: artifactResponse } =
              await github.rest.actions.listWorkflowRunArtifacts({
                owner,
                repo,
                run_id: runId,
                per_page: 100,
              });

            const artifact = artifactResponse.artifacts.find(candidate =>
              candidate.name === artifactName && !candidate.expired
            );

            if (!artifact) {
              core.setFailed(`O artifact ${artifactName} não foi encontrado.`);
              return;
            }

            const artifactUrl =
              `${process.env.GITHUB_SERVER_URL}/${owner}/${repo}` +
              `/actions/runs/${runId}/artifacts/${artifact.id}`;
            const shortSha = headSha.slice(0, 7);
            const body = [
              marker,
              '### Preview para Windows',
              '',
              `✅ Testes concluídos e versão autocontida gerada para \`${shortSha}\`.`,
              '',
              `**[Baixar instalador e versão portátil](${artifactUrl})**`,
              '',
              `O download contém \`MyFrame-win-Setup.exe\` e \`MyFrame-win-Portable.zip\`; não é necessário instalar o .NET SDK.`,
              '',
              '> ⚠️ Preview não assinado, gerado com o código deste PR. Execute somente alterações em que você confia.',
              '',
              `> É necessário entrar no GitHub. O artifact fica disponível por 30 dias. [Ver execução](${runUrl}).`,
            ].join('\n');

            const comments = await github.paginate(
              github.rest.issues.listComments,
              {
                owner,
                repo,
                issue_number: pullNumber,
                per_page: 100,
              }
            );
            const existingComment = comments.find(comment =>
              comment.user?.type === 'Bot' && comment.body?.includes(marker)
            );

            if (existingComment) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existingComment.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: pullNumber,
                body,
              });
            }
